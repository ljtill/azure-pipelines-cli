//! Azure DevOps REST client — authentication, HTTP transport, endpoint builders, and response models.

pub mod api_version;
pub mod auth;
pub mod endpoints;
pub mod http;
pub mod models;
pub mod wiql;
